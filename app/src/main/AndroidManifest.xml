<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.saner">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.round.RoundAvatarActivity"/>
        <activity android:name=".ui.photo.ShowPhotoActivity"/>
        <activity
            android:name=".ui.photo.SelectedActivity"
            android:theme="@style/AppTheme_NotToolbar"/>
        <activity android:name=".ui.photo.ClipImageActivity">
        </activity>
        <activity android:name=".ui.xfermode.XfermodeActivity">
        </activity>
        <activity android:name=".ui.shader.ShaderActivity">
        </activity>
        <activity android:name=".ui.canvas.CanvasActivity">
        </activity>
    </application>

</manifest>